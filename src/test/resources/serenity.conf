drivers {
  windows {
    webdriver.gecko.driver = src/test/resources/webdriver/windows/geckodriver.exe
  }}

  webdriver {
    driver = firefox
    timeouts {
      implicitlywait = 5000
      fluentwait = 10000
    }
  }