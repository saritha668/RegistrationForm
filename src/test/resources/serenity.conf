drivers {
  windows {
    webdriver.gecko.driver = src/test/resources/webdriver/windows/geckodriver.exe
  }}

  webdriver {
    driver = chrome
    timeouts {
      implicitlywait = 5000
      fluentwait = 10000
    }
  }